<Page x:Class="WpfApp1.Pages.ProductList"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:WpfApp1.Pages"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="ProductList" Background="{StaticResource MainBackgroundBrush}">
    <Page.Resources>
        <Style x:Key="ProductItemStyle" TargetType="Border">
            <Setter Property="Background" Value="{StaticResource MainBackgroundBrush}"/>
            <Setter Property="BorderBrush" Value="Gray"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="CornerRadius" Value="3"/>
            <Setter Property="Padding" Value="5"/>
            <Setter Property="Margin" Value="4"/>

            <Style.Triggers>
                <!--Скидка >12%-->
                <DataTrigger Binding="{Binding IsHighDiscount}" Value="True">
                    <Setter Property="Background" Value="#F4A460"/>
                </DataTrigger>
                <!--Нет товара-->
                <DataTrigger Binding="{Binding IsOutOfStock}" Value="True">
                    <Setter Property="Background" Value="LightBlue"/>
                </DataTrigger>
            </Style.Triggers>
        </Style>
    </Page.Resources>
    
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="40"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <StackPanel Grid.Row="0" x:Name="spFinder" Orientation="Horizontal" Visibility="Visible" Margin="10" VerticalAlignment="Center" HorizontalAlignment="Left">
            <TextBlock Text="Поиск:" VerticalAlignment="Center" Margin="0,0,5,0" Foreground="Black"/>
            <TextBox x:Name="tbSearch" Width="125" TextChanged="tbSearch_TextChanged"/>
            <TextBlock Text="Сортировка:" VerticalAlignment="Center" Margin="10,0,5,0" Width="70" HorizontalAlignment="Left"/>
            <ComboBox x:Name="cmbSort" Width="95" SelectionChanged="cmbSort_SelectionChanged">
                <ComboBoxItem Content="Price asc"/>
                <ComboBoxItem Content="Price desc"/>
                <ComboBoxItem Content="Discount asc"/>
                <ComboBoxItem Content="Discount desc"/>
                <ComboBoxItem Content="Count asc"/>
                <ComboBoxItem Content="Count desc"/>
            </ComboBox>
            <TextBlock Text="Производитель:" VerticalAlignment="Center" Margin="10,0,5,0" Width="70" HorizontalAlignment="Left"/>
            <ComboBox x:Name="cmbFilter" Width="130" SelectionChanged="cmbFilter_SelectionChanged"/>
            <Button Visibility="Collapsed" Content="Edit" x:Name="btnEdit" Margin="80,0,0,0" Width="50" Click="btnEdit_Click"/>
            <Button Visibility="Collapsed" Content="Add" x:Name="btnAdd" Margin="10,0,0,0" Width="50" Click="btnAdd_Click"/>
        </StackPanel>
        
        

        <ListBox Grid.Row="1" x:Name="lbProducts" HorizontalContentAlignment="Stretch" ScrollViewer.HorizontalScrollBarVisibility="Disabled">
            <ListBox.ItemTemplate>
                <DataTemplate>
                    <Border Style="{StaticResource ProductItemStyle}">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="150"/>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="120"/>
                            </Grid.ColumnDefinitions>
                            <Image Grid.Column="0" Height="120" Width="140" Stretch="Uniform" Source="{Binding ImagePath}"/>
                            <StackPanel Grid.Column="1" Margin="10,0,10,0" VerticalAlignment="Center">
                                <StackPanel Orientation="Horizontal" Margin="0,0,0,5">
                                    <TextBlock Text="{Binding Categories.CategoryName}" FontWeight="Bold" FontSize="15"/>
                                    <TextBlock Text=" | " FontWeight="Bold" FontSize="15"/>
                                    <TextBlock Text="{Binding ProductName}" FontWeight="Bold" FontSize="15"/>
                                </StackPanel>
                                <TextBlock Text="{Binding Description}" TextWrapping="Wrap" Margin="0,0,0,3"/>
                                <StackPanel Orientation="Horizontal" Margin="0,0,0,2">
                                    <TextBlock Text="Производитель: " FontWeight="SemiBold"/>
                                    <TextBlock Text="{Binding Producers.ProducerName}"/>
                                </StackPanel>
                                <StackPanel Orientation="Horizontal" Margin="0,0,0,2">
                                    <TextBlock Text="Поставщик: " FontWeight="SemiBold"/>
                                    <TextBlock Text="{Binding Suppliers.SupplierName}"/>
                                </StackPanel>
                                <StackPanel Orientation="Horizontal" Margin="0,0,0,2">
                                    <TextBlock Text="Единица измерения: " FontWeight="SemiBold"/>
                                    <TextBlock Text="{Binding Units.Name}"/>
                                </StackPanel>
                                <StackPanel Orientation="Horizontal" Margin="0,0,0,2">
                                    <TextBlock Text="Количество на складе: " FontWeight="SemiBold"/>
                                    <TextBlock Text="{Binding Count}"/>
                                </StackPanel>
                                <StackPanel Orientation="Horizontal" Margin="0,5,0,0">
                                    <TextBlock Text="Цена: " FontWeight="Bold" VerticalAlignment="Center"/>
                                    <TextBlock x:Name="TxtOldPrice" Text="{Binding Price, StringFormat={}{0:N2} руб.}" Foreground="Red" TextDecorations="Strikethrough" Margin="0,0,10,0"  VerticalAlignment="Center"/>
                                    <TextBlock x:Name="TxtActualPrice" Text="{Binding ActualPrice, StringFormat={}{0:N2} руб.}" Foreground="Black" FontWeight="Bold" FontSize="14" VerticalAlignment="Center"/>
                                </StackPanel>
                            </StackPanel>
                            <StackPanel Grid.Column="2" VerticalAlignment="Center" HorizontalAlignment="Center">
                                <TextBlock Text="Действующая" HorizontalAlignment="Center" FontSize="11"/>
                                <TextBlock Text="скидка" HorizontalAlignment="Center" FontSize="11"/>
                                <TextBlock Text="{Binding Discount, StringFormat={}{0}%}" FontWeight="Bold" FontSize="18" HorizontalAlignment="Center" Margin="0,5,0,0"/>
                            </StackPanel>
                        </Grid>
                    </Border>
                    <DataTemplate.Triggers>
                        <DataTrigger Binding="{Binding Discount}" Value="0">
                            <Setter TargetName="TxtOldPrice" Property="Visibility" Value="Collapsed"/>
                            <Setter TargetName="TxtActualPrice" Property="Text" Value="{Binding Price, StringFormat={}{0:N2} руб.}"/>
                        </DataTrigger>
                        <DataTrigger Binding="{Binding Discount}" Value="{x:Null}">
                            <Setter TargetName="TxtOldPrice" Property="Visibility" Value="Collapsed"/>
                            <Setter TargetName="TxtActualPrice" Property="Text" Value="{Binding Price, StringFormat={}{0:N2} руб.}"/>
                        </DataTrigger>
                    </DataTemplate.Triggers>
                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>
    </Grid>
</Page>
