<Page x:Class="WpfApp1.Pages.Auth"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:WpfApp1.Pages"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="Авторизация" Background="{StaticResource MainBackgroundBrush}">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="200"/>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="200"/>
        </Grid.ColumnDefinitions>
        <StackPanel Grid.Column="1" HorizontalAlignment="Center" VerticalAlignment="Center">
            <TextBlock Text="Авторизация" FontSize="20" FontWeight="Bold" HorizontalAlignment="Center" />
            <TextBlock Text="Login:" Margin="5,0,0,0" FontSize="14"/>
            <TextBox x:Name="tbLogin" Height="30" Width="250" Margin="0,0,0,10"/>
            <TextBlock Text="Password:" Margin="5,0,0,0" FontSize="14"/>
            <PasswordBox x:Name="pbPassword" Height="30" Width="250" Margin="0,0,0,10"/>
            <StackPanel Grid.Column="1" Orientation="Horizontal" HorizontalAlignment="Center" VerticalAlignment="Center">
                <Button x:Name="btnEnter"  Margin="5,15,5,5" Content="Войти" Height="35" Width="120" Click="btnEnter_Click" Background="{StaticResource AccentActionBrush}" />
                <Button x:Name="btnGuest"  Margin="5,15,5,5" Content="Войти как гость" Height="35" Width="120" Click="btnGuest_Click" />
            </StackPanel>
        </StackPanel>
        
    </Grid>
</Page>
